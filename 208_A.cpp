#include<stdio.h>
#include<algorithm>
using namespace std;

int main()
{
	int num;
	scanf("%d",&num);
	int n=num,i;
	int a[n],b[n];
	while(num--)
	{
		scanf("%d",&a[i]);
		b[i]=a[i];
	}
	sort(b,b+n);
	count=1;
	while(n--)
	{

	}
}